# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target '统一封装版本' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'AFNetworking'
pod 'LeanCloud'
pod 'Masonry'
pod 'SDWebImage'

  # Pods for 统一封装版本

  target '统一封装版本Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '统一封装版本UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
